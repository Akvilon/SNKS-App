import { Theme } from ".";

export const getMainColor = (theme: Theme) => theme.colors.main;
export const getMainBackground = (theme: Theme) => theme.colors.background;
export const getMainShadow = (theme: Theme) => theme.colors.mainShadow;
export const getMainText = (theme: Theme) => theme.colors.text;
